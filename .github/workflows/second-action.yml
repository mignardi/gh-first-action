name: Second Workflow
on: push #push a commit
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Get Code
        uses: actions/checkout@v3 #download the code in the repo
      - name: Send message
        run: echo "U did a push"

