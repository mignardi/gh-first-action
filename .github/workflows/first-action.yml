name: First Workflow
on: workflow_dispatch #manually
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print msgs
        run: |
          echo "Hello worlds 1"
          echo "Hello worlds 2"
    
